import 'package:flutter/material.dart';

class StaticticsView extends StatelessWidget {
  const StaticticsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFFF9F0E8),
      appBar: AppBar(
        backgroundColor: Colors.transparent,
        elevation: 0,
      ),
      body: Container(), // Keep the body content
    );
  }
}
